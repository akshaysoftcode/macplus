apiVersion: v1
kind: Pod
metadata:
  name: test-latest-tag
  labels:
    test: kyverno-violation
spec:
  containers:
    - name: nginx
      image: nginx:latest   # VIOLATION: mutable tag, should be denied
      resources:
        limits:
          memory: "128Mi"
          cpu: "100m"
      securityContext:
        runAsNonRoot: true
