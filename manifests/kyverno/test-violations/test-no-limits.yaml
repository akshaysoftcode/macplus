apiVersion: v1
kind: Pod
metadata:
  name: test-no-limits
  labels:
    test: kyverno-violation
spec:
  containers:
    - name: nginx
      image: nginx:1.27
      securityContext:
        runAsNonRoot: true
      # VIOLATION: no resources.limits block at all - should be denied
