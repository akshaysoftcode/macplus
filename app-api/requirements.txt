fastapi==0.104.1
uvicorn[standard]==0.24.0
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 reads bcrypt.__about__.__version__, which bcrypt>=4.1 removed.
# Pin bcrypt explicitly or passlib silently breaks with a misleading
# "password cannot be longer than 72 bytes" error even on short passwords.
bcrypt==4.0.1
pydantic==2.5.0

# --- INTENTIONALLY OUTDATED (SCA demo target) ---
# PyYAML 5.3.1 -> CVE-2020-14343 (arbitrary code execution via full_load on untrusted input)
PyYAML==5.3.1
# requests 2.31.0 -> CVE-2024-35195 (verify=False persists across a
# Session's later requests even when not explicitly re-set - a real,
# current CVE). Originally pinned to 2.19.1, but that version's own
# idna<2.8 constraint directly conflicts with FastAPI's anyio dependency
# (idna>=2.8) - genuinely unresolvable without changing one of the two,
# so this is bumped just far enough to resolve while keeping a real CVE.
requests==2.31.0